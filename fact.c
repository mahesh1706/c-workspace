#include<stdio.h>
int main()
{
    int n,i,fact;
    fact=1;
    printf("Enter a number:");
    scanf("%d",n);
    for (i=i; i<=n; ++i)
    {
      fact=fact*i;  /* code */
    }
    printf("Fact of num %d",fact);
    return 0;
    
}